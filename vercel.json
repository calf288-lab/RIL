{
  "buildCommand": "vite build && esbuild api/index.ts --bundle --platform=node --packages=external --format=esm --outfile=api/index.mjs && rm -f api/index.ts",
  "outputDirectory": "dist/public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/manus-storage/(.*)", "destination": "/api" },
    { "source": "/((?!assets/|photos/|favicon.svg).*)", "destination": "/index.html" }
  ]
}
